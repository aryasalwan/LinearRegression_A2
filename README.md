# LinearRegression_A2

## Setup Steps:
- Run the following to initialise virtual enviroment(If mac)
   > source .venv/bin/active
- Run the following to initialise virtual enviroment(If Windows)
   > .venv\Scripts\activate
- Then run this to install the required libs.
  > pip install -r requirements.txt
